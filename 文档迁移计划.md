# VuePress到VitePress迁移计划

## 迁移背景与原因

### 当前状况
我们的文档站点 `ai4energy/ai4energy.github.io` 目前使用 VuePress 作为文档生成工具。虽然 VuePress 曾经是非常好的选择，但随着前端技术的发展，我们面临以下挑战：

1. **构建速度慢**：VuePress 基于 webpack，构建速度较慢，特别是当文档量增加时
2. **开发体验差**：热更新慢，开发效率低
3. **技术栈老旧**：VuePress 使用较老的 Vue 2.x 技术栈
4. **维护困难**：社区活跃度下降，新功能较少

### 为什么要迁移到 VitePress
VitePress 是 VuePress 团队基于 Vite 开发的新一代静态站点生成器，具有以下优势：

1. **极致性能**：基于 Vite，启动速度秒级，HMR 瞬间完成
2. **现代技术栈**：基于 Vue 3，使用最新的前端技术
3. **官方支持**：Vue 团队维护，社区活跃
4. **开箱即用**：内置搜索、代码高亮等功能
5. **轻量简洁**：专门为 Markdown 文档优化

### 仓库拆分的原因
目前所有文档集中在 `Ai4EDocs` 一个仓库中，存在以下问题：
1. **维护复杂**：不同模块的文档混合在一起
2. **权限管理困难**：无法为不同模块设置独立的贡献者权限
3. **部署风险**：修改一处文档可能导致整个站点重新部署
4. **团队协作不便**：多人同时修改容易产生冲突

## 技术背景知识

### 什么是 VuePress
VuePress 是一个基于 Vue.js 的静态网站生成器，专门用于编写技术文档。它能够将 Markdown 文件转换成静态网页。

### 什么是 VitePress
VitePress 是 VuePress 的下一代版本，基于 Vite 构建，提供了更快的开发体验和更好的性能。

### 什么是 Vite
Vite 是新一代前端构建工具，由 Vue 的作者尤雨溪开发，特点是快速的冷启动和即时的热更新。

### 静态网站生成器
静态网站生成器是一种工具，它读取你的内容（通常是 Markdown 文件）和模板，然后生成最终的 HTML、CSS 和 JavaScript 文件。这些文件可以直接托管在任何 Web 服务器上。

## 当前系统的代码生成逻辑

### 当前 GitHub Actions 工作流 (.github/workflows/*.yml)

典型的 VuePress 部署工作流如下：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]  # 当推送到 main 分支时触发

jobs:
  deploy:
    runs-on: ubuntu-latest  # 在 Ubuntu 环境运行

    steps:
    - name: Checkout code  # 检出代码
      uses: actions/checkout@v3

    - name: Setup Node.js  # 安装 Node.js 环境
      uses: actions/setup-node@v3
      with:
        node-version: '16'  # 使用 Node.js 16

    - name: Install dependencies  # 安装依赖包
      run: npm ci

    - name: Build website  # 构建网站
      run: npm run build  # 执行 package.json 中的 build 命令
      env:
        BASE_URL: /  # 设置基础 URL

    - name: Deploy to GitHub Pages  # 部署到 GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}  # GitHub 提供的安全令牌
        publish_dir: ./dist  # 构建输出目录
```

这个工作流的工作原理：
1. 当有人推送代码到 main 分支时，GitHub 会自动运行这个工作流
2. 首先检出最新的代码
3. 安装 Node.js 环境
4. 安装项目依赖（通常在 package.json 中定义）
5. 运行构建命令生成静态网站文件
6. 将生成的文件部署到 GitHub Pages

### package.json 中的脚本
VuePress 项目的 package.json 通常包含：
```json
{
  "scripts": {
    "dev": "vuepress dev docs",  // 启动开发服务器
    "build": "vuepress build docs"  // 构建生产版本
  }
}
```

## 迁移任务分解

### Phase 1: 现状分析与准备工作
**目标**：全面了解当前系统，为迁移做好充分准备

#### 任务1.1：检查当前 VuePress 配置
- [ ] 查找并备份现有的配置文件
  - `docs/.vuepress/config.js` 或 `config.ts`
  - `package.json` 文件
  - `.github/workflows` 目录中的所有 YAML 文件
- [ ] 记录当前的文件夹结构
  - 主要的 Markdown 文件位置
  - 图片、PDF 等静态资源位置
  - 自定义 CSS 或 JS 文件
- [ ] 检查使用的 VuePress 插件
  - 例如搜索插件、PWA 插件等
  - 检查是否有自定义组件

#### 任务1.2：学习 VitePress 基础知识
**重要提醒**：这部分需要认真完成，因为 VitePress 和 VuePress 有很多不同之处

- [ ] 阅读 VitePress 官方文档的入门部分
- [ ] 了解 `.md` 文件如何包含 Vue 组件
- [ ] 学习 VitePress 配置文件格式
- [ ] 了解主题定制方法
- [ ] 练习创建简单的 VitePress 站点

#### 任务1.3：制定详细的时间表
- [ ] 根据团队成员可用时间，合理安排各阶段任务
- [ ] 为每个任务设置明确的验收标准
- [ ] 预留足够的时间进行测试

### Phase 2: ai4energy/ai4energy.github.io 仓库迁移
**目标**：将主文档站点从 VuePress 迁移到 VitePress

#### 任务2.1：在本地环境测试迁移
- [ ] 创建一个新的 VitePress 项目
  ```bash
  mkdir temp-vitepress-test
  cd temp-vitepress-test
  npm init vitepress
  ```
- [ ] 将少量文档文件复制过来，测试是否兼容
- [ ] 测试基本功能（导航、搜索、代码块等）
- [ ] 确认 Markdown 语法兼容性
- [ ] 记录发现的问题和解决方法

#### 任务2.2：配置 VitePress
- [ ] 创建 VitePress 配置文件 `docs/.vitepress/config.js`
- [ ] 复制现有的导航菜单配置（注意语法可能需要调整）
- [ ] 设置网站标题、描述等基本信息
- [ ] 配置基础样式（颜色、字体等）
- [ ] 测试本地运行效果：`npm run docs:dev`

#### 任务2.3：内容迁移
- [ ] 将所有 Markdown 文件从 VuePress 项目复制到 VitePress 项目
- [ ] 检查并修复可能存在的语法不兼容问题
- [ ] 更新图片和链接路径（相对路径可能会有问题）
- [ ] 验证所有内部链接是否有效
- [ ] 测试代码块显示效果

#### 任务2.4：主题和样式定制
- [ ] 应用自定义 CSS 样式（如果有）
- [ ] 设置站点图标（favicon）
- [ ] 配置面包屑导航
- [ ] 测试移动端显示效果
- [ ] 验证打印样式

#### 任务2.5：更新 GitHub Actions 部署脚本
**这是最重要的一步**：更新 `.github/workflows/deploy.yml` 文件

```yaml
name: Deploy VitePress site to Pages

on:
  push:
    branches: [ main ]  # 当推送到 main 分支时触发

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整的 Git 历史记录

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20  # 使用较新的 Node.js 版本

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Install dependencies
        run: npm ci  # 安装依赖

      - name: Build with VitePress
        run: npm run docs:build  # 构建 VitePress 网站

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/.vitepress/dist  # VitePress 输出目录

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

同时需要更新 `package.json` 的脚本：
```json
{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  }
}
```

#### 任务2.6：全面测试
- [ ] 在本地测试所有页面是否正常显示
- [ ] 检查所有链接是否正确指向
- [ ] 测试搜索功能
- [ ] 验证代码块高亮
- [ ] 测试移动端浏览体验

### Phase 3: Ai4EDocs 仓库拆分规划
**目标**：将单一文档仓库拆分为多个独立的文档仓库

#### 任务3.1：文档内容分析
- [ ] 创建电子表格，列出所有文档文件
- [ ] 按功能模块分类（比如：安装指南、API 文档、教程、FAQ 等）
- [ ] 识别哪些文档应该放在一起
- [ ] 确定哪些是公共文档（多个模块都需要）

#### 任务3.2：新仓库规划
- [ ] 为每个文档模块创建仓库名称（例如：ai4edocs-install、ai4edocs-api、ai4edocs-tutorials）
- [ ] 设计每个仓库的基本结构
- [ ] 规划仓库之间的关系和链接

#### 任务3.3：制定拆分策略
- [ ] 确定拆分顺序（哪个先拆分）
- [ ] 规划如何处理跨仓库的引用
- [ ] 设计统一的品牌风格（Logo、颜色等）

### Phase 4: 拆分实施
**目标**：按照规划创建新仓库并迁移文档

#### 任务4.1：创建新仓库
- [ ] 在 GitHub 上创建新的文档仓库
- [ ] 为每个仓库初始化 VitePress 项目
- [ ] 设置相同的配置模板（保持风格一致）
- [ ] 配置基本的 GitHub Actions

#### 任务4.2：文档迁移
- [ ] 按照之前的分类，将文档分配到对应的新仓库
- [ ] 为每个仓库配置导航和菜单
- [ ] 更新内部链接（确保指向正确的仓库）
- [ ] 测试每个新仓库的独立运行

#### 任务4.3：建立关联
- [ ] 在主站点添加到各个文档模块的链接
- [ ] 设置跨仓库的引用机制（如果需要）
- [ ] 创建统一的文档导航页

### Phase 5: 测试与上线
**目标**：确保所有系统正常运行

#### 任务5.1：全面功能测试
- [ ] 测试所有文档页面的显示效果
- [ ] 验证搜索功能正常工作
- [ ] 检查移动端兼容性
- [ ] 测试跨仓库链接有效性
- [ ] 验证构建流程是否自动化

#### 任务5.2：用户验收测试
- [ ] 邀请团队成员试用新文档站点
- [ ] 收集反馈意见
- [ ] 修复发现的问题

#### 任务5.3：正式上线
- [ ] 更新 DNS 或重定向规则
- [ ] 发布公告通知用户
- [ ] 监控新站点运行状态

## 常见问题与解决方案

### Markdown 语法兼容性问题
- VuePress 中的某些 Markdown 语法可能在 VitePress 中不支持
- 解决方案：逐一测试，必要时手动调整

### 图片路径问题
- 从 VuePress 迁移的图片路径可能需要调整
- 解决方案：批量替换路径，或调整配置

### 自定义组件迁移
- 如果使用了 Vue 组件，可能需要调整才能在 VitePress 中工作
- 解决方案：参考 VitePress 文档调整组件格式

### 搜索功能配置
- VitePress 有内置的 Algolia 搜索，需要重新配置
- 解决方案：要么使用默认的客户端搜索，要么申请 Algolia 配置

## 验收标准

### 技术要求
- [ ] 所有页面能够正常访问
- [ ] 加载速度比之前快至少 50%
- [ ] 移动端显示正常
- [ ] 搜索功能正常
- [ ] 代码高亮正常
- [ ] 所有链接有效

### 用户体验要求
- [ ] 界面清晰易用
- [ ] 导航结构合理
- [ ] 阅读体验良好
- [ ] 搜索准确有效

### 维护性要求
- [ ] 构建流程自动化
- [ ] 部署流程可靠
- [ ] 文档易于更新
- [ ] 问题定位容易

## 时间规划建议

**总计预估时间：3-4周**

- Week 1: 现状分析与技术准备
- Week 2: 主站点迁移测试
- Week 3: 完成主站点迁移和新仓库创建
- Week 4: 完成拆分和全面测试

**注意**：这个时间表是理想情况下的估计，请根据实际情况适当延长。

---
*重要提醒：请在开始任何重大更改前，务必备份当前的所有文件和配置。*